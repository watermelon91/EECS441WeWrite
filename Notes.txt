Event:

1. Timer fires
2. Reaches 16 characters (for both insert and delete)
3. Manual cursor location change

typedef BUFFER_LEN 16;
struct Buffer
{
	char contents[BUFFER_LEN];
	long startLocation;
	int lengthUsed;
	NSString *eventType
}